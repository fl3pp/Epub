<Project>
  <PropertyGroup>
    <IsTestProject>$(MSBuildProjectName.Contains('Test'))</IsTestProject>
    <Version>1.0.0.0</Version>
    <Platforms>AnyCPU;x64</Platforms>
    <IsPackable>False</IsPackable> <!-- Must be enabled inside projects -->
  </PropertyGroup>
</Project>